# 星榆中学 · 游戏逻辑线（当前版本）

> 整理时间：2026-07-09
> 本文档反映**代码当前真实状态**。标注「⚠️」的为剧情/文档与代码不一致或待修复项。
> 密码明细见 `密码.md`。

---

## 一、入口与全局框架

- **`start.html`**：标题菜单。点「新游戏」→ `SaveSystem.createSlot()`（开存档槽）、`GameTime.reset()`（周四19:00起）、`BGM.start()`、播过渡动画后 `go('map.html')`。「存档点 / 成就 / 退出」走 `SaveSystem`。
- **页面跳转**：各地图用 `go(url)`（带 CRT 过渡）跳转；跨页进度靠 `sessionStorage` 传递。
- **音乐**：`bgm.js` 的 `BGM`（恐怖氛围 + `startChase`/`tryPlaySwitchSound`/`playStair`/`playComputer`/`stopChase`）+ `start.html` 内嵌 `AmbientMusic`（庄严铺底）。首次按键/点击恢复被挂起的 `AudioContext`。

**关键进度状态（sessionStorage）**：
- `chaseOn`（追逐中）
- `breakerOff`（配电隔间总闸已落下，由 `map4` 写入）
- `redPressed`（三楼红按钮已按下，由 `map3` 写入）
- `chaserRel` / `xyzh_resume`（黑影偏移 / 玩家坐标，跨页续接）
- `map1_tp_tr`（迷宫核心传送标记，由 `mp5` 写入）
- `mapReturn`（返回标记，各楼楼梯/电脑间回跳用）
- `zhangUnlocked`（政教办工号锁，已被 `ZYJ-001` 直接验证取代）

---

## 二、探索主线（按区域 / 楼层）

### 一楼 `map.html`
| 区域 / ID | 类型 / 密码 | 内容要点 |
|---|---|---|
| 医务室 `pwd_jiaowu` | pw `1705` | 红药瓶 / 监控线索 |
| 监控机房 `pwd_monitor` | pw `1700` | 触发 jump-scare，红药瓶监视 17:00 |
| 档案储物间 `doc_archive` | pw `4829` ⚠️旧文档记 `1145` | 处分记录（含「是否将学校论坛归档」选择框，见第六节 dead code） |
| 门卫值班室 `doc_wang` | pw `8080` ⚠️旧文档记 `1901` | 王伯笔记（三楼密码锁前三位 3-5-4） |
| 公告栏 `doc_bulletin` | doc | 校规 |
| 102教室·电脑 `room_102` | computer | 跳 `index.html` 论坛（带 `mapReturn='102'`） |
| 大门 `gate` | pw `8127` | 输对 → 直接坏结局（逃离） |
| 侧门电闸 `side_breaker` | doc | **结局判定点**：见第四节 |
| 侧门通道 `sideStairs` | sideStairs | 需 `breakerOff==='1'` 才能进入 → `mp5.html`（地下配电迷宫） |
| 上二楼 | stairs | → `map2.html` |

### 二楼 `map2.html`
- 政教主任办公室 `doc_zhang`：`locked`，工号 `ZYJ-001` → 教师内部安全手册（含医务室保险柜 1705、工作日志）。⚠️ 工号无任何游戏内线索（见第六节）。
- 教师办公室 / 茶水间 / 闲置画室：普通笔记。
- 休息 `bed`：调整游戏内时间。
- 上三楼 `stairs_up`：`locked`，密码 `3548`（来自王伯笔记「3-5-4-?」+ 关停年 08）。

### 三楼 `map3.html`（二楼输对 3548 解锁）
- 301 / 302 / 303 教室：林晓周记（第八篇「把那台机器关掉」）、空教室、废弃班级。
- 卫生间：布满灰尘的镜子 → 镜中门（`pendingMap4`）通往 **`map4.html`**。
- 实验储物柜：违规实验记录碎片（核心仪器必须关掉）。
- **生化实验室 `doc_lab`**：核心仪器 NM-2005 + 红色按钮（最终抉择）：
  - 电闸**未**落下（`!breakerOff`）→ 直接死亡结局。
  - 电闸已落下 → 存活，`redPressed='1'`，黑影在生化实验室生成并开始追逐。

### 配电隔间 `map4.html`（三楼卫生间镜中门进入）
- 数个分闸 + 中央总控台，逐个拉下后总闸落下。
- 拉下总闸 → 写 `breakerOff='1'`，关闭文本后传送回 `map3.html` 卫生间门外（`xyzh_resume`）。
- 纯开关，无密码。

### 地下室 / 地下配电迷宫 `mp5.html`（侧门进，需 `breakerOff`）
- 四角 4 个电闸 + 中央总控台 `masterSwitch`。
- **进入即 `chaseOn=1`，黑影生成并 A\* 追逐**，红闪 + 玩家提速，被抓 → 坏结局。
- 拉下 4 分闸 → 总控台激活 → 交互总控台：写 `chaseOn`+`map1_tp_tr`、播 `BGM.startChase()`、300ms 后跳回 **`map.html` 右上角**，0.5s 后黑影也到右上角继续追。
- **隐秘通道 `hiddenDoor`**（右上角死角，16×26）：
  - 四闸未全开：完全融入墙砖，不可见、不可交互。
  - 四闸全开后：门缝微弱发光，靠近可交互。
  - 当前交互仅弹「你试着推了推这扇门……门锁上了。」**不跳转**（原设计为跳 `map6`，已改为伏笔）。

### 实验区走廊 `map6.html`（门字形环廊）⚠️ 当前不可达
- 场景内容：门字形大走廊 + 中央 NM-2005 实验核心仪器、残破黑板、散落实验报告、布满灰尘的镜子、左侧小门、底部封条门（标注「地下配电室」）。
- **当前无法从游戏内进入**：全代码无任何 `go('map6.html')` 调用；`mp5` 隐秘通道与 `map6` 底部封条门（类型 `exit`，标注「地下配电室」）都被改为弹「门锁上了」，入口与出口同时被堵死 → **scene 处于 orphaned 状态**。
- NM-2005 机器编码验证（`machine` 交互）：⚠️ `MACHINE_CODE` 被初始化为空字符串，`genMachineCode()` 已定义但未调用，输入验证实际永远失败，仪器关停无法完成（见第六节）。

---

## 三、追逐系统（黑影）

- **触发来源**：① 进 `mp5` 立即 `chaseOn`；② `map3` 红按钮（电闸已落）关闭文本后生成黑影。
- **行为**：A\* 绕墙追玩家，`CHASE_CHASER_RATIO`（mp5 / map2 / map3 = 0.8，map.html / map6 = 0.6），`CHASE_SPEED_MUL` 各场景均为 1.7，`chaserDelay` 延迟启动；跨页用 `saveChaserRel`/`xyzh_resume` 续接；`chaseOn` 时玩家提速 + 红闪。
- **抓捕即死**：被黑影碰到 → 定格「你死了」→ `bad_ending.html`（`map`/`map2`/`map3`/`mp4`/`mp5`/`map6` 均如此）。

---

## 四、结局

- **好结局 `good_ending.html`**：判定点在 `map.html` 的 **`side_breaker`（doc 分支）**，条件 `breakerOff==='1' && redPressed==='1'`（配电隔间总闸已落 + 三楼红按钮已按）→ 毁掉仪器、真相重见天日。
- **坏结局 `bad_ending.html`**（多路径）：
  ① 大门输对 `8127`（逃离）；
  ② `side_breaker` 已拉闸（`breakerOff`）但未按红钮（`!redPressed`）→ 直接出去；
  ③ 任意楼层被黑影抓住；
  ④ `mp5` 被抓；
  ⑤ `map3` 红按钮在电闸未落时按下 → 直接死亡。
  终屏打字 `GAME OVER` 完成后提示「点击任意位置返回首页」（手动）。
- ⚠️ **`map6` 的 NM-2005 关停线目前不可达**：因 map6 无法进入 + 机器编码空值 bug，这条潜在的好结局支线实际无法触发。

---

## 五、论坛 / 日志支线

- **`index.html`**：学校论坛，帖子需学号 `0817` 解锁。原帖「不为人知的真相」在 3.4 被改为「[此段内容已被删除]」+ 张老师黑色涂黑块 + 已注销用户红色回复（3.4 线索链大改）。学生档案处密码框为死胡同，需走「忘记密码」安全问答答案 `118`。
- **`logs.html`**：任务日志系统，首屏固定「林晓·周记」，王伯等面板、密码区（学号 `0817`）、涂黑线索（关停 08 年）、三楼大门密码 `3548` 推理。
- **`bad_ending.html` / `bgm_player.html` 等**：结局演出与音乐播放辅助页。

---

## 六、已知问题 / 待修复（按优先级）

1. **⚠️ map6 不可达（关键）**：`mp5` 隐秘通道、`map6` 底部封条门两处都改为「门锁上了」，且无其它入口，导致 `map6` 整张场景（含 NM-2005 关停线）无法进入。若要打通，需至少开放一侧跳转（建议：四闸全开后 `mp5` 隐秘通道 → `map6`，且进入 `map6` 时清除追逐状态）。
2. **⚠️ NM-2005 机器编码空值 bug**：`map6` 中 `MACHINE_CODE=''`，`genMachineCode()` 未被调用，仪器关停永远失败。需在某处（如档案/镜中线索）生成并暴露 20 位编码（末 4 位 `2005`）或在进入时调用 `genMachineCode()`。
3. **工号 `ZYJ-001` 无游戏内线索**：锁界面只提示「教师工号验证」，玩家无从得知（文档/周记/笔记中均未给出）。
4. **档案「论坛归档」选择框 dead code**：归档交互写在 `handleInteraction` 的 `doc` 分支 `openDialog` 之后，但 `doc_archive` 为 `password` 类型，输对后 `submitPw` 直接开处分记录，归档选择框永不触发。
5. **密码与旧文档冲突（已纠正）**：`逻辑线.md` / `密码.md` 旧值 档案室 `1145`、门卫 `1901` 已过时，游戏内实际为 `4829` / `8080`，以 `密码.md` 为准。
